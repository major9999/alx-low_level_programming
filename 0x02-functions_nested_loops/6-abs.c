#include "main.h"

/**
 * _abs - print the absolute integer value of a number.
 * @n: the number for the test.
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
