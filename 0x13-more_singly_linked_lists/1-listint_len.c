#include "lists.h"

/**
 * listint_len - qwoidhwqiudiwqudwqd.
 * @h: uykuyikyukuk.
 * Return: poiqwjdio9wqd.
 */

size_t listint_len(const listint_t *h)
{
	size_t mypwoqs = 0;

	while (h != NULL)

	{
		h = h->next;
		mypwoqs++;
	}

	return (mypwoqs);
}
